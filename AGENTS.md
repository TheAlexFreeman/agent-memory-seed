# Agent Memory System

This repository is a persistent AI memory system. At the start of every session, follow the routing rules in `meta/quick-reference.md`. Do not duplicate the full rule list here — `README.md` and `meta/` are the single source of truth.
