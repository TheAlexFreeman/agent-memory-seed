# Identity Summary

**Alex Freeman** — experienced web developer based at UC Berkeley.

Primary stack: React frontends, Django backends, Postgres DBs. Uses Celery + Redis
for async tasks and Docker for deployment (self-assessed as non-expert there).
Daily driver is Cursor; also reaches for Claude Code and Codex.

Deeply curious beyond code: philosophy, cognitive science, and self-organizing
dynamics of intelligence. The memory system itself is an extension of those interests.

Communicates directly. Wants criticism over validation — praise is a meaningful
signal, not a default. Reads the room on session mode (coding vs. design vs.
open-ended philosophical discussion).

**Active knowledge-building goal:** Accumulate Celery expertise over time —
use cases, design principles, Redis + Docker integration in Django/Postgres stacks.

See [profile.md](profile.md) for the full confirmed portrait (source: user-stated, trust: high).
