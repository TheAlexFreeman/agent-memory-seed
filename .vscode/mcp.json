{
  "servers": {
    "agent-memory": {
      "type": "stdio",
      "command": "${workspaceFolder}/.venv/Scripts/python",
      "args": ["${workspaceFolder}/engram_mcp/memory_mcp.py"],
      "env": {
        "AGENT_MEMORY_ROOT": "${workspaceFolder}"
      }
    }
  }
}
